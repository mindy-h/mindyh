---
import figma from '../assets/figma.svg';
import cursor from '../assets/cursor.svg';
import claude from '../assets/claude.svg';
import ai from '../assets/ai.svg';
import clarity from '../assets/clarity_ico.svg';
import key from '../assets/key.svg';
---

<section id="process" class="process">
	<div class="container process__container">
		<h2 class="process__title fade-in">How I work</h2>
		<p class="process__intro fade-in">
			I focus on building systems that scale. My process emphasizes clarity, collaboration, and 
			tools that get out of the way so the work can speak for itself.
		</p>

		<div class="process__grid">
			<article class="process__card fade-in" data-parallax data-speed="0.15">
				<div class="process__icon">
					<img src={figma.src} alt="Figma" />
				</div>
				<h3 class="process__heading">Design Systems</h3>
				<p class="process__text">
					Building scalable component libraries in Figma that maintain consistency across products 
					and teams. Variables, auto-layout, and documentation built in.
				</p>
			</article>

			<article class="process__card fade-in" data-parallax data-speed="0.2" style="transition-delay: 0.1s;">
				<div class="process__icon">
					<img src={cursor.src} alt="Cursor" />
				</div>
				<h3 class="process__heading">Rapid Prototyping</h3>
				<p class="process__text">
					Using Cursor and Claude to quickly validate ideas with working prototypes. 
					Ship fast, learn faster, iterate with confidence.
				</p>
			</article>

			<article class="process__card fade-in" data-parallax data-speed="0.25" style="transition-delay: 0.2s;">
				<div class="process__icon">
					<img src={ai.src} alt="AI Tools" />
				</div>
				<h3 class="process__heading">AI-Assisted Design</h3>
				<p class="process__text">
					Leveraging AI tools to accelerate workflows—from generating design variations 
					to automating repetitive tasks so I can focus on strategy.
				</p>
			</article>

			<article class="process__card fade-in" data-parallax data-speed="0.15" style="transition-delay: 0.3s;">
				<div class="process__icon">
					<img src={clarity.src} alt="Clarity" />
				</div>
				<h3 class="process__heading">User Research</h3>
				<p class="process__text">
					Analytics and user feedback drive every decision. Microsoft Clarity, heat maps, 
					and direct interviews reveal what's actually working.
				</p>
			</article>

			<article class="process__card fade-in" data-parallax data-speed="0.2" style="transition-delay: 0.4s;">
				<div class="process__icon">
					<img src={key.src} alt="Key" />
				</div>
				<h3 class="process__heading">Stakeholder Alignment</h3>
				<p class="process__text">
					Translating business goals into design requirements. Regular check-ins, 
					clear documentation, and proactive communication keep everyone aligned.
				</p>
			</article>

			<article class="process__card fade-in" data-parallax data-speed="0.25" style="transition-delay: 0.5s;">
				<div class="process__icon">
					<img src={claude.src} alt="Claude" />
				</div>
				<h3 class="process__heading">Process Documentation</h3>
				<p class="process__text">
					Creating clear, repeatable processes that teams can follow without me. 
					Documenting decisions so they don't have to be remade.
				</p>
			</article>
		</div>
	</div>
	<div class="process__blur"></div>
</section>

<style>
	.process {
		position: relative;
		padding: 4rem 0 5rem;
		background: linear-gradient(180deg, rgba(26, 23, 18, 0) 0%, #0a0907 100%);
	}

	.process__blur {
		position: absolute;
		bottom: 0;
		left: 0;
		right: 0;
		height: 200px;
		background: linear-gradient(to top, #0a0907 0%, rgba(10, 9, 7, 0) 100%);
		pointer-events: none;
		z-index: 2;
	}

	.process__container {
		position: relative;
		z-index: 1;
	}

	.process__title {
		font-family: var(--font-display);
		font-size: clamp(2.25rem, 4vw, 3rem);
		color: var(--color-accent);
		margin-bottom: 1.5rem;
		text-align: center;
	}

	.process__intro {
		font-size: 1.125rem;
		line-height: 1.6;
		text-align: center;
		max-width: 800px;
		margin: 0 auto 3.5rem;
		color: rgba(243, 234, 208, 0.9);
	}

	.process__grid {
		display: grid;
		grid-template-columns: repeat(3, minmax(0, 1fr));
		gap: 1.5rem;
	}

	.process__card {
		border: 1px solid rgba(251, 255, 0, 0.15);
		border-radius: 1.25rem;
		padding: 2rem 1.75rem;
		background: rgba(0, 0, 0, 0.4);
		backdrop-filter: blur(8px);
		transition: all 0.3s ease;
		display: flex;
		flex-direction: column;
		gap: 1rem;
	}

	.process__card:hover {
		border-color: rgba(251, 255, 0, 0.4);
		transform: translateY(-4px) !important;
		box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);
	}

	.process__icon {
		width: 56px;
		height: 56px;
		display: flex;
		align-items: center;
		justify-content: center;
		background: rgba(251, 255, 0, 0.1);
		border-radius: 12px;
		padding: 12px;
	}

	.process__icon img {
		width: 100%;
		height: 100%;
		object-fit: contain;
		filter: brightness(0) saturate(100%) invert(98%) sepia(89%) saturate(2298%) hue-rotate(359deg) brightness(105%) contrast(103%);
	}

	.process__heading {
		font-size: 1.35rem;
		font-weight: 700;
		color: #fff;
		margin: 0;
	}

	.process__text {
		font-size: 0.95rem;
		line-height: 1.6;
		color: rgba(243, 234, 208, 0.85);
		margin: 0;
	}

	@media (max-width: 1024px) {
		.process__grid {
			grid-template-columns: repeat(2, minmax(0, 1fr));
		}
	}

	@media (max-width: 640px) {
		.process {
			padding: 3rem 0 4rem;
		}

		.process__intro {
			font-size: 1rem;
			margin-bottom: 2.5rem;
		}

		.process__grid {
			grid-template-columns: 1fr;
		}

		.process__card {
			padding: 1.5rem 1.25rem;
		}
	}
</style>

