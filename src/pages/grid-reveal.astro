---
import Layout from '../layouts/Layout.astro';
import Navigation from '../components/Navigation.astro';
import Footer from '../components/Footer.astro';
import { Image, getImage } from 'astro:assets';
import statue from '../assets/main-image.png';
import torn from '../assets/torn.svg';
import outlineTitle from '../assets/outline_title.svg';
import arrow from '../assets/arrow.svg';
import clouds from '../assets/clouds.png';
import noise from '../assets/bgtexture.jpg';
import layer from '../assets/hero_bg.svg';

const cloudsOpt = await getImage({ src: clouds, format: 'webp', quality: 80, width: 1920, height: 1080 });
const noiseOpt = await getImage({ src: noise, format: 'webp', quality: 80, width: 1920, height: 1080 });
---

<Layout title="Grid Reveal • Mindy House">
	<Navigation />
	<main class="page" style={`--noise: url(${noiseOpt.src}); --clouds: url(${cloudsOpt.src}); --layer: url(${layer.src});`}>
		<section class="hero">
			<div class="hero-bg">
				<div class="hero-grad"></div>
				<div class="hero-layer"></div>
				<div class="hero-noise"></div>
			</div>
			<div class="hero-content">
				<h1 class="hero-title">
					<span>Designer.</span>
					<br />
					<span class="accent">Listener.</span>
					<br />
					<span>Process builder.</span>
				</h1>
				<p class="hero-sub">I'm Mindy. Product and visual designer with 10+ years in enterprise and e-commerce, currently at Anheuser-Busch.</p>
				<div class="hero-arrow" aria-hidden="true">
					<img src={arrow.src} alt="" />
				</div>
			</div>
		</section>

		<div class="divider">
			<img src={torn.src} alt="" />
		</div>

		<section class="how">
			<div class="how-bg">
				<div class="how-clouds"></div>
				<div class="how-noise"></div>
			</div>
			<div class="how-inner">
				<div class="how-title">
					<img src={outlineTitle.src} alt="How I work" />
				</div>
				<div class="how-left">
					<p class="how-lead">The best design work happens when we have a seat at the strategy table.</p>
				</div>
				<div class="how-right">
					<p>Designers need a voice at the strategy level to deliver the best work. That means digging into the why alongside engineers and product owners, not just executing decisions.</p>
				</div>
				<div class="how-statue">
					<div class="statue-glow"></div>
					<Image 
						src={statue} 
						alt="Statue" 
						width={600}
						height={800}
						format="webp"
						quality={85}
						loading="lazy"
						decoding="async"
					/>
				</div>
			</div>
		</section>
	</main>
	<Footer />
</Layout>

<style>
	:global(body) {
		background: #0b0c12;
	}

	.page {
		color: #f0f0f0;
		overflow-x: hidden;
		font-family: 'Inter', system-ui, -apple-system, sans-serif;
	}

	.hero {
		position: relative;
		min-height: 100vh;
		display: flex;
		align-items: center;
		justify-content: center;
		text-align: center;
		padding: 140px 24px 120px;
	}

	.hero-bg {
		position: absolute;
		inset: 0;
		overflow: hidden;
	}

	.hero-grad {
		position: absolute;
		inset: 0;
		background:
			radial-gradient(circle at 20% 25%, rgba(136, 210, 44, 0.08), transparent 38%),
			radial-gradient(circle at 72% 8%, rgba(92, 120, 255, 0.25), transparent 55%),
			linear-gradient(180deg, #0f1016 0%, #0b0d17 30%, #0a0c16 60%, #05060c 100%);
	}

	.hero-layer {
		position: absolute;
		left: 50%;
		top: 0;
		width: 1800px;
		height: 900px;
		transform: translateX(-50%);
		background-image: var(--layer);
		background-size: cover;
		background-position: center;
		opacity: 0.6;
		mix-blend-mode: screen;
	}

	.hero-vignette {
		position: absolute;
		inset: 0;
		background: linear-gradient(180deg, rgba(5, 6, 12, 0.45) 0%, rgba(5, 6, 12, 0) 25%, rgba(5, 6, 12, 0) 70%, rgba(5, 6, 12, 0.75) 100%);
		pointer-events: none;
	}

	.hero-noise {
		position: absolute;
		inset: 0;
		background-image: var(--noise);
		opacity: 0.14;
		mix-blend-mode: soft-light;
	}

	.hero-content {
		position: relative;
		max-width: 640px;
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 18px;
	}

	.hero-title {
		font-family: 'Grenze Gotisch', 'Times New Roman', serif;
		font-size: clamp(3.2rem, 5vw, 4.2rem);
		line-height: 1.08;
		color: #ffffff;
		text-shadow: 0 10px 35px rgba(0, 0, 0, 0.5);
	}

	.accent {
		color: #c192ff;
	}

	.hero-sub {
		font-size: 1rem;
		line-height: 2rem;
		color: #dcdde4;
		max-width: 520px;
	}

	.hero-arrow img {
		width: 54px;
		height: 54px;
		object-fit: contain;
		filter: brightness(0) saturate(100%) invert(82%) sepia(14%) saturate(1079%) hue-rotate(227deg) brightness(98%) contrast(104%);
	}

	.divider img {
		width: 100%;
		display: block;
		transform: translateY(-1px);
	}

	.how {
		position: relative;
		padding: 120px 32px 240px;
		overflow: hidden;
	}

	.how-bg {
		position: absolute;
		inset: 0;
		background: linear-gradient(180deg, #07070f 0%, #0b0b13 50%, #090910 100%);
	}

	.how-clouds {
		position: absolute;
		inset: -10% -20% -20% -10%;
		background-image: var(--clouds);
		background-size: cover;
		background-position: center;
		mix-blend-mode: screen;
		opacity: 0.28;
		filter: saturate(0.6);
	}

	.how-noise {
		position: absolute;
		inset: 0;
		background-image: var(--noise);
		opacity: 0.14;
		mix-blend-mode: soft-light;
	}

	.how-vignette {
		position: absolute;
		inset: 0;
		background: linear-gradient(180deg, rgba(5, 6, 12, 0.5) 0%, rgba(5, 6, 12, 0) 35%, rgba(5, 6, 12, 0) 70%, rgba(5, 6, 12, 0.8) 100%);
		pointer-events: none;
	}

	.how-inner {
		position: relative;
		max-width: 1320px;
		margin: 0 auto;
		display: grid;
		grid-template-columns: repeat(12, 1fr);
		gap: 24px;
		align-items: center;
		padding-top: 30px;
		padding-bottom: 120px;
	}

	.how-title {
		grid-column: 1 / span 12;
		text-align: center;
	}

	.how-title img {
		width: 560px;
		max-width: 100%;
		display: block;
		margin: 0 auto 12px;
		filter: brightness(0) saturate(100%) invert(82%) sepia(14%) saturate(1079%) hue-rotate(227deg) brightness(98%) contrast(104%);
	}

	.how-left {
		grid-column: 1 / span 5;
		padding-left: 12px;
	}

	.how-right {
		grid-column: 8 / span 5;
		max-width: 420px;
	}

	.how-lead {
		font-family: 'Grenze Gotisch', 'Times New Roman', serif;
		font-size: 2.3rem;
		line-height: 1.22;
		color: #f6f6f7;
	}

	.how-left p,
	.how-right p {
		font-size: 1.08rem;
		line-height: 1.7;
		color: #f4f4f4;
	}

	.how-statue {
		position: absolute;
		left: 50%;
		top: 150px;
		transform: translateX(-50%);
		width: min(520px, 78vw);
		filter: drop-shadow(0 28px 65px rgba(0, 0, 0, 0.6));
		z-index: 2;
	}

	.how-statue img {
		width: 100%;
		max-width: 100%;
		display: block;
		position: relative;
		z-index: 2;
	}

	.statue-glow {
		position: absolute;
		inset: -16% -12%;
		background: radial-gradient(ellipse at center, rgba(155, 129, 255, 0.35), transparent 70%);
		filter: blur(18px);
		z-index: 1;
	}

	.how::before,
	.how::after {
		content: '';
		position: absolute;
		left: 0;
		right: 0;
		height: 260px;
		pointer-events: none;
	}

	.how::before {
		top: 0;
		background: linear-gradient(180deg, rgba(5, 6, 12, 0.9) 0%, rgba(5, 6, 12, 0) 100%);
	}

	.how::after {
		bottom: 0;
		background: linear-gradient(0deg, rgba(5, 6, 12, 0.9) 0%, rgba(5, 6, 12, 0) 100%);
	}
	@media (max-width: 1024px) {
		.hero {
			padding-top: 120px;
		}

		.how-inner {
			grid-template-columns: repeat(8, 1fr);
			row-gap: 28px;
		}

		.how-left {
			grid-column: 1 / span 8;
			order: 2;
		}

		.how-right {
			grid-column: 1 / span 8;
			order: 3;
		}

		.how-statue {
			grid-column: 2 / span 6;
			order: 1;
		}
	}

	@media (max-width: 640px) {
		.hero {
			padding: 110px 18px 100px;
		}

		.hero-title {
			font-size: clamp(2.2rem, 10vw, 3.2rem);
		}

		.hero-sub {
			font-size: 0.95rem;
		}

		.how-inner {
			grid-template-columns: repeat(6, 1fr);
		}

		.how-statue {
			grid-column: 1 / span 6;
		}

		.how-left,
		.how-right {
			grid-column: 1 / span 6;
		}

		.how-title img {
			width: 340px;
		}
	}
</style>

